services:
  react-app:
    build:
      context: ./react-app
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: unless-stopped

  fast-api:
    build:
      context: ./fast-api
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    restart: unless-stopped
